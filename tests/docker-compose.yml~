version: '3.4'
services:
  
  pdspi-guidance-example-test:
    build:
      context: tests
    command: ["-s", "-vv", "-x"]
    depends_on:
      - pdspi-guidance-example
      - pdspi-test-pds-server

  pdspi-test-pds-server:
    build:
      context: tests/pds-server-mock
